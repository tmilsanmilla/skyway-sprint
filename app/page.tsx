'use client';
import {FormEvent,useCallback,useEffect,useRef,useState} from 'react';
import {createBrowserClient} from '@supabase/ssr';
type Kind='gem'|'boost'|'car'|'log'|'rock'|'barrel'; type Item={id:number;lane:number;y:number;kind:Kind};
type GameMode='normal'|'hardcore'|'impossible';
type PlayerReport={id:number;user_id:string;report_type:string;message:string;status:string;created_at:string};
type AdminUser={user_id:string;email:string;username:string|null;role:'main'|'co_admin'};
type Leader={rank:number;username:string;high_score:number};
const supabase=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);
function Obstacle({kind}:{kind:Kind}){
 if(kind==='gem')return <span>♦</span>;
 if(kind==='boost')return <span>+100</span>;

 if(kind==='barrel')return <div className="barrel-shape"><i/><b/><em/></div>;
 if(kind==='car')return <div className="car-shape"><i className="windshield"/><i className="light left"/><i className="light right"/><i className="wheel left"/><i className="wheel right"/><b/></div>;
 if(kind==='log')return <div className="log-shape"><i/><b/><em/></div>;
 return <div className="rock-shape"><i/><b/><em/></div>;
}
export default function Home(){
 const [lane,setLane]=useState(2),[items,setItems]=useState<Item[]>([]),[score,setScore]=useState(0),[gems,setGems]=useState(0),[highScore,setHighScore]=useState(0),[gemBump,setGemBump]=useState(false),[hearts,setHearts]=useState(3),[wave,setWave]=useState(1),[running,setRunning]=useState(false),[paused,setPaused]=useState(false),[wavePause,setWavePause]=useState(false),[waveMessage,setWaveMessage]=useState(''),[over,setOver]=useState(false),[flash,setFlash]=useState(''),[invincible,setInvincible]=useState(false),[shopOpen,setShopOpen]=useState(false),[shopStatus,setShopStatus]=useState(''),[leaderboardOpen,setLeaderboardOpen]=useState(false),[leaders,setLeaders]=useState<Leader[]>([]);
 const id=useRef(0),last=useRef(0),userIdRef=useRef<string|null>(null),gemsRef=useRef(0),scoreRef=useRef(0),highScoreRef=useRef(0),invincibleUntilRef=useRef(0),state=useRef({lane,running,paused}); state.current={lane,running,paused};gemsRef.current=gems;scoreRef.current=score;highScoreRef.current=highScore;
 const [authReady,setAuthReady]=useState(false),[guest,setGuest]=useState(false),[userEmail,setUserEmail]=useState<string|null>(null),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirmPassword,setConfirmPassword]=useState(''),[authMode,setAuthMode]=useState<'signin'|'signup'>('signin'),[authBusy,setAuthBusy]=useState(false),[authMessage,setAuthMessage]=useState('');
 const [settingsOpen,setSettingsOpen]=useState(false),[adminOpen,setAdminOpen]=useState(false),[isAdmin,setIsAdmin]=useState(false),[adminRole,setAdminRole]=useState<string|null>(null),[adminTab,setAdminTab]=useState<'reports'|'admins'>('reports'),[admins,setAdmins]=useState<AdminUser[]>([]),[adminTarget,setAdminTarget]=useState(''),[adminStatus,setAdminStatus]=useState(''),[reports,setReports]=useState<PlayerReport[]>([]),[copyStatus,setCopyStatus]=useState(''),[reportType,setReportType]=useState('Bug'),[reportMessage,setReportMessage]=useState(''),[reportStatus,setReportStatus]=useState(''),[reportBusy,setReportBusy]=useState(false);
 const [username,setUsername]=useState(''),[usernameInput,setUsernameInput]=useState(''),[usernameRequired,setUsernameRequired]=useState(false),[usernameStatus,setUsernameStatus]=useState(''),[newPassword,setNewPassword]=useState(''),[passwordStatus,setPasswordStatus]=useState('');
 const [editUsername,setEditUsername]=useState(false),[editPassword,setEditPassword]=useState(false);
 const [mode,setMode]=useState<GameMode>('normal');
 const maxHearts=mode==='impossible'?1:3;
 const announceWave=useCallback((number:number)=>{setWaveMessage(`WAVE ${number}`);setWavePause(true);setTimeout(()=>{setWaveMessage('');setWavePause(false)},1250)},[]);
 const reset=useCallback(()=>{setLane(2);setItems([]);setScore(0);setHearts(mode==='impossible'?1:3);setWave(1);setOver(false);setPaused(false);setInvincible(false);invincibleUntilRef.current=0;setRunning(true);last.current=0;announceWave(1)},[announceWave,mode]);
 const backToMenu=()=>{setRunning(false);setPaused(false);setOver(false);setItems([]);setScore(0);setWave(1);setHearts(mode==='impossible'?1:3);setInvincible(false);invincibleUntilRef.current=0};
 const move=useCallback((d:number)=>{if(state.current.running&&!state.current.paused)setLane(v=>Math.max(0,Math.min(4,v+d)))},[]);
 useEffect(()=>{const key=(e:KeyboardEvent)=>{const target=e.target as HTMLElement|null;if(target?.matches('input, textarea, select, [contenteditable="true"]'))return;if(['ArrowLeft','a','A'].includes(e.key)){e.preventDefault();move(-1)}if(['ArrowRight','d','D'].includes(e.key)){e.preventDefault();move(1)}if(e.key===' '&&state.current.running){e.preventDefault();setPaused(v=>!v)}if(e.key==='Enter'&&!state.current.running)reset()};addEventListener('keydown',key);return()=>removeEventListener('keydown',key)},[move,reset]);
 useEffect(()=>{if(!running||paused||wavePause)return;let raf=0,prev=performance.now();const tick=(now:number)=>{const dt=Math.min(32,now-prev);prev=now;if(now-last.current>Math.max(330,980-wave*55)){last.current=now;const r=Math.random(),danger=Math.min(.79,.54+wave*.025),gemThreshold=mode==='impossible'?.86:mode==='hardcore'?.9:.94;let kind:Kind;if(r<danger)kind=(['car','log','rock','barrel']as Kind[])[Math.floor(Math.random()*4)];else if(r>gemThreshold)kind='gem';else kind='boost';setItems(v=>{const blocked=new Set(v.map(x=>x.lane));const lanes=[0,1,2,3,4].filter(l=>!blocked.has(l));if(!lanes.length)return v;const spawnLane=lanes[Math.floor(Math.random()*lanes.length)];return [...v,{id:id.current++,lane:spawnLane,y:-10,kind}]})}setItems(old=>old.flatMap(item=>{const speedFactor=item.kind==='car'?1.28:item.kind==='rock'?.6:item.kind==='barrel'?1.08:1;const n={...item,y:item.y+(.04+wave*.0052)*speedFactor*dt};if(n.lane===state.current.lane&&n.y>65&&n.y<91){if(n.kind==='gem'){const total=gemsRef.current+1;gemsRef.current=total;setGems(total);setGemBump(false);requestAnimationFrame(()=>setGemBump(true));setTimeout(()=>setGemBump(false),500);if(userIdRef.current)void supabase.rpc('increment_player_gems').then(({data,error})=>{if(error){console.error('Could not save gem:',error.message);return}if(typeof data==='number'){gemsRef.current=data;setGems(data)}})}else if(n.kind==='boost'){setScore(v=>v+Math.round(100*(1+wave*.01)*(mode==='impossible'?2:mode==='hardcore'?1.5:1)))}else if(invincibleUntilRef.current>Date.now()){setFlash('shield');setTimeout(()=>setFlash(''),120);return[]}else{setHearts(v=>{const h=v-(n.kind==='rock'?2:1);if(h<=0){setRunning(false);setOver(true);if(guest){setGems(0);gemsRef.current=0}else{const best=Math.max(highScoreRef.current,scoreRef.current);highScoreRef.current=best;setHighScore(best);if(userIdRef.current)void supabase.rpc('save_player_high_score',{new_score:best}).then(({data,error})=>{if(error){console.error('Could not save high score:',error.message);return}if(typeof data==='number'){highScoreRef.current=data;setHighScore(data)}})}}return Math.max(0,h)});const blockedLanes=new Set(old.filter(x=>x.id!==n.id&&x.kind!=='gem'&&x.kind!=='boost'&&x.y>50&&x.y<96).map(x=>x.lane));const safe=[0,1,2,3,4].filter(l=>l!==n.lane&&!blockedLanes.has(l)).sort((a,b)=>Math.abs(a-state.current.lane)-Math.abs(b-state.current.lane));if(safe.length)setLane(safe[0]);setPaused(true);setFlash('life-lost');setTimeout(()=>{setFlash('');setPaused(false)},480);return[n]}return[]}return n.y<108?[n]:[]}));setScore(v=>v+Math.max(1,Math.round((dt/12)*(1+wave*.01)*(mode==='impossible'?2:mode==='hardcore'?1.5:1))));raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick);return()=>cancelAnimationFrame(raf)},[running,paused,wavePause,wave,guest,mode,maxHearts]);
 useEffect(()=>{if(!running)return;const next=Math.floor(score/2250)+1;if(next!==wave){setWave(next);if(mode==='normal')setHearts(v=>Math.min(maxHearts,v+1));announceWave(next)}},[score,running,wave,announceWave,mode,maxHearts]);
 useEffect(()=>{const applySession=async(session:Awaited<ReturnType<typeof supabase.auth.getSession>>['data']['session'])=>{const user=session?.user??null;userIdRef.current=user?.id??null;setUserEmail(user?.email??null);if(user){const [{data:stats,error:statsError},{data:profile},{data:admin},{data:role}]=await Promise.all([supabase.from('player_stats').select('total_gems,high_score').eq('user_id',user.id).maybeSingle(),supabase.from('player_profiles').select('username,username_changed_at').eq('user_id',user.id).maybeSingle(),supabase.rpc('is_admin'),supabase.rpc('get_admin_role')]);if(statsError)console.error('Could not load account stats:',statsError.message);if(stats){gemsRef.current=stats.total_gems;highScoreRef.current=stats.high_score;setGems(stats.total_gems);setHighScore(stats.high_score)}else{const {error}=await supabase.from('player_stats').insert({user_id:user.id});if(error)console.error('Could not create account stats:',error.message)}if(profile){setUsername(profile.username);setUsernameInput(profile.username);setUsernameRequired(false)}else setUsernameRequired(true);setIsAdmin(Boolean(admin));setAdminRole(role)}else{setUsername('');setUsernameRequired(false);setIsAdmin(false);setAdminRole(null)}setAuthReady(true)};supabase.auth.getSession().then(({data})=>void applySession(data.session));const {data}=supabase.auth.onAuthStateChange((event,session)=>{if(event==="PASSWORD_RECOVERY"){setSettingsOpen(true);setPasswordStatus("Verified. Enter your new password below.")}void applySession(session)});return()=>data.subscription.unsubscribe()},[]);
 const submitAuth=async(e:FormEvent)=>{e.preventDefault();setAuthBusy(true);setAuthMessage('');if(authMode==='signup'){if(password!==confirmPassword){setAuthMessage('Passwords do not match.');setAuthBusy(false);return}const {error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin}});setAuthMessage(error?error.message:'Check your email to confirm your account, then return here to sign in.')}else{const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setAuthMessage(error.message)}setAuthBusy(false)};
 const sendPasswordReset=async(targetEmail:string,setStatus:(message:string)=>void)=>{if(!targetEmail){setStatus('Enter your email address first.');return}setStatus('Sending recovery email…');const {error}=await supabase.auth.resetPasswordForEmail(targetEmail,{redirectTo:window.location.origin});setStatus(error?error.message:'Recovery email sent. Open its link to verify your account and choose a new password.')};
 const signOut=async()=>{setRunning(false);setPaused(false);setWavePause(false);setItems([]);setOver(false);setScore(0);setSettingsOpen(false);setAdminOpen(false);setShopOpen(false);setLeaderboardOpen(false);setGuest(false);userIdRef.current=null;if(userEmail){setUserEmail(null);await supabase.auth.signOut()}};
 const playGuest=()=>{setGuest(true);setGems(0);setHighScore(0);gemsRef.current=0;highScoreRef.current=0};
 const submitReport=async(e:FormEvent)=>{e.preventDefault();if(!userIdRef.current)return;setReportBusy(true);setReportStatus('');const {error}=await supabase.from('player_reports').insert({user_id:userIdRef.current,report_type:reportType,message:reportMessage.trim()});if(error)setReportStatus(error.message);else{setReportStatus('Report sent. Thank you for helping improve Skyway Sprint!');setReportMessage('')}setReportBusy(false)};
 const saveUsername=async(e:FormEvent)=>{e.preventDefault();setUsernameStatus('');const {data,error}=await supabase.rpc('set_player_username',{new_username:usernameInput});if(error)setUsernameStatus(error.message);else{setUsername(data.username);setUsernameInput(data.username);setUsernameRequired(false);setEditUsername(false);setUsernameStatus('Username saved. You can change it again in 30 days.')}};
 const changePassword=async(e:FormEvent)=>{e.preventDefault();setPasswordStatus('');const {error}=await supabase.auth.updateUser({password:newPassword});if(error)setPasswordStatus(error.message);else{setNewPassword('');setEditPassword(false);setPasswordStatus('Password updated successfully.')}};
 const loadReports=async()=>{const {data}=await supabase.from('player_reports').select('*').neq('status','resolved').order('created_at',{ascending:false});setReports((data??[]) as PlayerReport[]);setAdminTab('reports');setPaused(true);setAdminOpen(true)};
 const loadAdmins=async()=>{const {data,error}=await supabase.rpc('list_admins');if(error)setAdminStatus(error.message);else setAdmins((data??[]) as AdminUser[]);setAdminTab('admins')};
 const manageAdmin=async(target:string,action:'add'|'promote'|'demote'|'remove')=>{setAdminStatus('');const {error}=await supabase.rpc('manage_admin',{target,admin_action:action});if(error){setAdminStatus(error.message);return}setAdminTarget('');setAdminStatus(action==='remove'?'Co-admin removed.':action==='promote'?'Admin promoted to main admin.':action==='demote'?'Admin changed to co-admin.':'Co-admin added.');await loadAdmins()};
 const resolveReport=async(id:number)=>{const {error}=await supabase.rpc('resolve_player_report',{report_id:id});if(error){setCopyStatus(error.message);return}setReports(v=>v.filter(r=>r.id!==id))};
 const copyOpenReports=async()=>{const open=reports.filter(r=>r.status!=='resolved');if(open.length===0){setCopyStatus('No open reports to copy.');return}const text=open.map((r,index)=>`REPORT ${index+1}\nType: ${r.report_type}\nDate: ${new Date(r.created_at).toLocaleString()}\nPlayer: ${r.user_id}\nStatus: ${r.status}\n\n${r.message}`).join('\n\n--------------------\n\n');await navigator.clipboard.writeText(text);setCopyStatus(`${open.length} open report${open.length===1?'':'s'} copied.`);setTimeout(()=>setCopyStatus(''),2200)};
 const activatePowerup=(kind:'score_boost'|'invincibility'|'heart')=>{if(kind==='score_boost'){setScore(v=>v+1000);setShopStatus('+1,000 score activated!')}else if(kind==='heart'){if(mode!=='normal'){setShopStatus('Glass hearts cannot be healed.');return}setHearts(v=>Math.min(maxHearts,v+1));setShopStatus('One heart restored!')}else{const until=Math.max(Date.now(),invincibleUntilRef.current)+8000;invincibleUntilRef.current=until;setInvincible(true);setShopStatus('Invincible for 8 seconds!');setTimeout(()=>{if(invincibleUntilRef.current<=Date.now())setInvincible(false)},until-Date.now()+50)}};
 const buyPowerup=async(kind:'score_boost'|'invincibility'|'heart',cost:number)=>{setShopStatus('');if(kind==='heart'&&mode!=='normal'){setShopStatus('Glass hearts cannot be healed.');return}if(gemsRef.current<cost){setShopStatus('Not enough gems.');return}if(guest){const remaining=gemsRef.current-cost;gemsRef.current=remaining;setGems(remaining);activatePowerup(kind);setTimeout(()=>{setShopOpen(false);setPaused(false)},500);return}const {data,error}=await supabase.rpc('buy_powerup',{powerup:kind});if(error){setShopStatus(error.message);return}if(typeof data==='number'){gemsRef.current=data;setGems(data)}activatePowerup(kind);setTimeout(()=>{setShopOpen(false);setPaused(false)},500)};
 const loadLeaderboard=async()=>{const {data}=await supabase.rpc('get_leaderboard');setLeaders((data??[]) as Leader[]);setLeaderboardOpen(true)};
 if(!authReady)return <main className="auth-shell"><div className="auth-card loading">Loading Skyway Sprint…</div></main>;
 if(!userEmail&&!guest)return <main className="auth-shell"><section className="auth-card"><div className="auth-logo">S</div><p>FIVE LANES. NO BRAKES.</p><h1>{authMode==='signin'?'WELCOME BACK':'JOIN THE RUN'}</h1><form onSubmit={submitAuth} autoComplete="on"><label>Email<input id="login-email" name="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="runner@example.com" required autoComplete="email"/></label><label>Password<input id="login-password" name="password" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 6 characters" minLength={6} required autoComplete={authMode==='signin'?'current-password':'new-password'}/></label>{authMode==="signup"&&<label>Confirm Password<input id="confirm-password" name="confirm-password" type="password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} placeholder="Enter the same password again" minLength={6} required autoComplete="new-password"/></label>}{authMessage&&<div className="auth-message" role="status">{authMessage}</div>}<button disabled={authBusy}>{authBusy?'PLEASE WAIT…':authMode==='signin'?'SIGN IN →':'CREATE ACCOUNT →'}</button></form>{authMode==="signin"&&<button className="forgot-button" onClick={()=>sendPasswordReset(email,setAuthMessage)}>FORGOT PASSWORD?</button>}<button className="auth-switch" onClick={()=>{setAuthMode(v=>v==='signin'?'signup':'signin');setAuthMessage('')}}>{authMode==='signin'?'New runner? Create an account':'Already registered? Sign in'}</button><div className="guest-divider"><span>OR</span></div><button className="guest-button" onClick={playGuest}>PLAY AS GUEST</button><small className="guest-note">Guest gems disappear after every run.</small></section></main>;
 return <main className={`game-shell ${flash}`}><div className="game-layout"><nav className="game-actions"><button className="action-leaderboard" onClick={()=>{void loadLeaderboard();setPaused(true)}}><span className="trophy-icon">🏆</span><b>LEADERBOARD</b></button><button className="action-shop" onClick={()=>{setShopOpen(true);setPaused(true);setShopStatus("")}}><span className="cart-icon"><i/><i/><i/></span><b>SHOP</b></button>{!guest&&<button className="action-settings" onClick={()=>{setSettingsOpen(true);setPaused(true)}}><span>⚙</span><b>SETTINGS</b></button>}{isAdmin&&!guest&&<button className="action-admin" onClick={loadReports}><span>★</span><b>ADMIN</b></button>}</nav><section className="game-card" aria-label="Skyway Sprint runner game"><header className="topbar"><div className="brand"><span>S</span><div><b>SKYWAY</b><small>SPRINT</small></div></div><div className="stats"><div><small>SCORE</small><strong>{score.toString().padStart(6,'0')}</strong></div><div className="high-score-inline"><small>HIGH SCORE</small><strong>{guest?"—":highScore.toLocaleString()}</strong></div><div className={`gem-total ${gemBump?"bump":""}`}><small>{guest?"RUN GEMS":"ALL-TIME GEMS"}</small><strong className="gold">● {gems}</strong>{gemBump&&<em>+1</em>}</div><div><small>WAVE</small><strong>{wave}</strong></div></div><div className="account"><span>{guest?"GUEST RUNNER":username||userEmail}</span></div></header><div className="playfield">{waveMessage&&<div className="wave-announcement"><small>GET READY</small><strong>{waveMessage}</strong></div>}<div className="sky"><i/><i/><i/></div><div className="horizon">{[0,1,2,3,4].map(n=><span key={n}/>)}</div><div className="road">{[0,1,2,3].map(n=><i className={`line l${n}`} key={n}/>)}<div className="wave-chip">WAVE {wave}<small>SPEED ×{(1+(wave-1)*.12).toFixed(2)}</small></div>{items.map(x=><div key={x.id} className={`item ${x.kind}`} style={{left:`${(x.lane+.5)*20}%`,top:`${x.y}%`}} aria-label={x.kind}><Obstacle kind={x.kind}/></div>)}<div className={invincible?"runner invincible":"runner"} style={{left:`${(lane+.5)*20}%`}}><div className="head"/><div className="body"/><i className="arm a1"/><i className="arm a2"/><i className="leg g1"/><i className="leg g2"/><em/></div></div>{!running&&<div className="overlay"><p>{over?'RUN OVER':'FIVE LANES. NO BRAKES.'}</p><h1>{over?`${score.toLocaleString()} POINTS`:'DODGE. DASH. SURVIVE.'}</h1>{!over&&<div className="mode-select"><button className={mode==="normal"?"selected":""} onClick={()=>setMode("normal")}><b>NORMAL</b><small>3 hearts · healing enabled</small></button><button className={mode==="hardcore"?"selected":""} onClick={()=>setMode("hardcore")}><b>HARDCORE</b><small>3 glass hearts · 1.5× score</small></button><button className={mode==="impossible"?"selected":""} onClick={()=>setMode("impossible")}><b>IMPOSSIBLE</b><small>1 glass heart · 2× score</small></button></div>}<button onClick={reset}>{over?'RUN AGAIN':'START RUN'} <span>→</span></button>{over&&<button className="back-menu" onClick={backToMenu}>BACK TO MENU</button>}<small>← → / A D &nbsp; TO SWITCH LANES</small></div>}{paused&&<div className="overlay compact"><h1>PAUSED</h1><button onClick={()=>setPaused(false)}>KEEP RUNNING</button></div>}</div><footer><button onClick={()=>move(-1)} aria-label="Move left">←</button><p><b>SWITCH LANES</b><small>Use arrows, A / D, or tap</small></p><button onClick={()=>move(1)} aria-label="Move right">→</button><div className={`health ${mode!=="normal"?"glass":""}`} aria-label={`${hearts} hearts`}>{[0,1,2].map(n=><span key={n} className={hearts-n>=1?"":hearts-n>0?"partial":"lost"}>♥</span>)}</div><button className="pause" onClick={()=>running&&setPaused(v=>!v)} aria-label="Pause">{paused?'▶':'Ⅱ'}</button></footer></section>{(settingsOpen||usernameRequired)&&!guest&&<div className="report-backdrop" role="dialog" aria-modal="true"><section className="settings-modal">{!usernameRequired&&<button className="report-close" onClick={()=>{setSettingsOpen(false);setPaused(false)}}>×</button>}<p>PLAYER SETTINGS</p><h2>{usernameRequired?"CHOOSE A USERNAME":"SETTINGS"}</h2>{usernameRequired&&<div className="required-note">A username is required before you can play.</div>}{(usernameRequired||editUsername)?<form onSubmit={saveUsername}><label>USERNAME<input value={usernameInput} onChange={e=>setUsernameInput(e.target.value)} minLength={3} maxLength={20} pattern="[A-Za-z0-9_]+" placeholder="3-20 letters, numbers, underscores" required/></label>{usernameStatus&&<div className="report-status">{usernameStatus}</div>}<button>SAVE USERNAME</button>{!usernameRequired&&<button type="button" className="settings-cancel" onClick={()=>setEditUsername(false)}>CANCEL</button>}</form>:<button className="settings-action" onClick={()=>{setEditUsername(true);setUsernameStatus('')}}>CHANGE USERNAME</button>}{!usernameRequired&&<>{editPassword?<form onSubmit={changePassword}><label>NEW PASSWORD<input id="new-password" name="new-password" autoComplete="new-password" type="password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} minLength={6} required/></label>{passwordStatus&&<div className="report-status">{passwordStatus}</div>}<button>SAVE NEW PASSWORD</button><button type="button" className="forgot-settings" onClick={()=>sendPasswordReset(userEmail||"",setPasswordStatus)}>EMAIL ME A RECOVERY LINK</button><button type="button" className="settings-cancel" onClick={()=>setEditPassword(false)}>CANCEL</button></form>:<button className="settings-action" onClick={()=>{setEditPassword(true);setPasswordStatus('')}}>CHANGE PASSWORD</button>}<div className="settings-report"><h3>REPORT AN ISSUE</h3><form onSubmit={submitReport}><label>TYPE<select value={reportType} onChange={e=>setReportType(e.target.value)}><option>Bug</option><option>Gameplay problem</option><option>Account problem</option><option>Suggestion</option><option>Other</option></select></label><label>DETAILS<textarea value={reportMessage} onChange={e=>setReportMessage(e.target.value)} minLength={10} maxLength={1500} required/></label>{reportStatus&&<div className="report-status">{reportStatus}</div>}<button disabled={reportBusy}>{reportBusy?"SENDING…":"SEND REPORT"}</button></form></div></>}{!usernameRequired&&<button className="signout-settings" onClick={signOut}>SIGN OUT</button>}</section></div>}{shopOpen&&<div className="report-backdrop"><section className="powerup-modal"><button className="report-close" onClick={()=>{setShopOpen(false);setPaused(false)}}>×</button><p>GEM SHOP</p><h2>POWERUPS</h2><div className="powerup-grid"><button onClick={()=>buyPowerup("score_boost",8)}><b>+1,000 SCORE</b><span>♦ 8</span><small>Instant score boost</small></button><button onClick={()=>buyPowerup("invincibility",13)}><b>INVINCIBILITY</b><span>♦ 13</span><small>No damage for 8 seconds</small></button><button onClick={()=>buyPowerup("heart",5)}><b>RESTORE HEART</b><span>♦ 5</span><small>Recover one lost heart</small></button></div><strong className="shop-balance">BALANCE: ♦ {gems}</strong>{shopStatus&&<div className="report-status">{shopStatus}</div>}</section></div>}{leaderboardOpen&&<div className="report-backdrop"><section className="leaderboard-modal"><button className="report-close" onClick={()=>{setLeaderboardOpen(false);setPaused(false)}}>×</button><p>LEADERBOARD 1</p><h2>TOP RUNNERS</h2><div className="leader-list">{leaders.length===0?<div className="empty-reports">No scores yet.</div>:leaders.map(entry=><div key={entry.username+entry.rank} className={entry.username===username?"me":""}><b>#{entry.rank}</b><span>{entry.username}</span><strong>{entry.high_score.toLocaleString()}</strong></div>)}</div></section></div>}{adminOpen&&isAdmin&&<div className="report-backdrop"><section className="admin-inbox"><button className="report-close" onClick={()=>{setAdminOpen(false);setPaused(false)}}>×</button><p>ADMIN CONTROL</p><h2>{adminTab==="reports"?"REPORT INBOX":"ADMIN TEAM"}</h2><div className="admin-tabs"><button className={adminTab==="reports"?"active":""} onClick={()=>setAdminTab("reports")}>REPORTS</button><button className={adminTab==="admins"?"active":""} onClick={loadAdmins}>ADMINS</button></div>{adminTab==="reports"?<><div className="admin-toolbar"><button onClick={copyOpenReports}>COPY ALL OPEN REPORTS</button>{copyStatus&&<span>{copyStatus}</span>}</div><h3 className="inbox-heading">OPEN REPORTS <span>{reports.length}</span></h3><div className="report-list">{reports.length===0?<div className="empty-reports">No open reports.</div>:reports.map(r=><article key={r.id}><header><b>{r.report_type}</b><span>{r.status.toUpperCase()}</span></header><p>{r.message}</p><small>{new Date(r.created_at).toLocaleString()} · {r.user_id.slice(0,8)}</small><button onClick={()=>resolveReport(r.id)}>RESOLVE & DELETE</button></article>)}</div></>:<div className="admin-team">{adminRole==="main"&&<form onSubmit={e=>{e.preventDefault();void manageAdmin(adminTarget,"add")}}><label>ADD BY EMAIL OR USERNAME<input value={adminTarget} onChange={e=>setAdminTarget(e.target.value)} required placeholder="player@email.com or username"/></label><button>ADD CO-ADMIN</button></form>}{adminStatus&&<div className="report-status">{adminStatus}</div>}<div className="admin-list">{admins.map(a=><article key={a.user_id}><div><b>{a.username||"No username"}</b><small>{a.email}</small></div><span>{a.role==="main"?"MAIN ADMIN":"CO-ADMIN"}</span>{adminRole==="main"&&a.email!==userEmail&&<div className="admin-actions">{a.role==="co_admin"?<><button onClick={()=>manageAdmin(a.email,"promote")}>PROMOTE</button><button onClick={()=>manageAdmin(a.email,"remove")}>KICK</button></>:<button onClick={()=>manageAdmin(a.email,"demote")}>MAKE CO-ADMIN</button>}</div>}</article>)}</div></div>}</section></div>}</div></main>
}
